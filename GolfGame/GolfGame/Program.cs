namespace GolfGame
{
    internal class Program
    {
        private const string Path = @"..\..\..\InputData\test.txt";

        private static void Main()
        {
            //var outputData = new OutputData();
            //outputData.AddMatchedPair(new Coords(1, 2), new Coords(3, 4));
            //outputData.AddMatchedPair(new Coords(5, 6), new Coords(7, 8));

            //var inputData = FileManager.ReadInput(Path);
            //FileManager.WriteOutput(outputData);
        }
    }
}
